name: test
channels:
dependencies:
    # Base depends
    - python
    - pip
    
      # Testing
    - pytest
    - pytest-cov
    - codecov

    # dependencies
    - numpy
    - matplotlib
    - sphinx_rtd_theme

    # Pip-only installs
  - pip:
    - travis-sphinx

